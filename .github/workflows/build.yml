name: Build EXE

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install openpyxl pandas pyinstaller

      - name: Build EXE
        run: |
          pyinstaller --onefile --windowed --name "브이리뷰_변환기" main.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: 브이리뷰_변환기
          path: dist/브이리뷰_변환기.exe
